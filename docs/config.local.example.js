// Copiá este archivo como config.local.js (está en .gitignore: no se sube).
// Sirve solo para probar en tu máquina, sin tocar el repositorio público.
Object.assign(window.IR_CONFIG || (window.IR_CONFIG = {}), {
  supabaseUrl: "",
  supabaseAnonKey: "",
  aiProvider: "pollinations",
  aiKey: ""
});
